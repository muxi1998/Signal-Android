package com.mtkresearch.breeze.api

import android.app.Activity
import android.graphics.Rect

/**
 * Interface for Breeze UI integration hooks.
 * Defines the contract for interactions between the main app and Breeze module.
 *
 * Currently minimal for build compatibility.
 */
interface BreezeUiHook {
  // Empty interface for now as all UI hooks have been removed.
  // Kept for type compatibility in BreezeRegistry.
}
